[project]
name = "task1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.58.0",
    "pytest-playwright>=0.7.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
